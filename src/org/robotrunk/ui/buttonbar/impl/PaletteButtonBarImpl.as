package org.robotrunk.ui.buttonbar.impl {
	import flash.events.Event;

	public class PaletteButtonBarImpl extends AbstractButtonBarImpl {
		override protected function onButtonClick( ev:Event ):void {
			// No switching etc., just let the button do what it does.
		}

		public function PaletteButtonBarImpl() {
			super();
		}
	}
}
